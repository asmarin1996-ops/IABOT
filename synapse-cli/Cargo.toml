[package]
name = "synapse-cli"
version.workspace = true
edition.workspace = true
description = "CLI interactiva para SynapseAI"

[features]
default = []
# Cuerpo fisico: actuadores reales para Raspberry Pi (servos PCA9685 + motores DC)
rpi = ["synapse-hal/rpi"]
# Percepcion real (camara V4L2 + microfono ALSA) para Raspberry Pi
percepcion_real = ["synapse-hal/percepcion_real"]

[[bin]]
name = "synapse"
path = "src/main.rs"

[dependencies]
synapse-core = { path = "../synapse-core" }
synapse-memory = { path = "../synapse-memory" }
synapse-consciousness = { path = "../synapse-consciousness" }
synapse-hal = { path = "../synapse-hal" }
synapse-sim = { path = "../synapse-sim" }
tokio.workspace = true
mio = "1.2"
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
env_logger.workspace = true
log.workspace = true
toml.workspace = true
chrono.workspace = true
piper-tts-rs = { path = "../vendor/piper-tts-rs", version = "0.1.4" }
rodio = "0.22"
